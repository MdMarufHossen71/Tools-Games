import { CalendarDays, Gamepad2, NotebookPen, Wrench } from "lucide-react";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { useSettings } from "@/contexts/AppSettingsContext";
export default function Profile() { const { user, isAuthenticated } = useAuth(); const { t } = useSettings(); if (!isAuthenticated) return <div className="site-frame empty-page"><span className="icon-orb"><Wrench /></span><h1>{t("profile.signinTitle")}</h1><p>{t("profile.signinCopy")}</p><button className="primary-cta" onClick={() => startLogin()}>{t("profile.secureSignin")}</button></div>; const initials = user?.name?.slice(0, 1).toUpperCase() ?? "U"; return <div className="site-frame profile-page"><div className="profile-hero"><div className="profile-avatar">{initials}</div><div><p className="eyebrow">{t("profile.member")}</p><h1>{user?.name ?? t("profile.member")}</h1><p>{user?.email ?? t("settings.copy")}</p></div></div><div className="stat-grid">{[{ label: t("profile.tools"), value: "0", icon: Wrench }, { label: t("profile.games"), value: "0", icon: Gamepad2 }, { label: t("profile.notes"), value: "0", icon: NotebookPen }, { label: t("profile.streak"), value: "0", icon: CalendarDays }].map(({ label, value, icon: Icon }) => <article key={label}><Icon size={18} /><span>{label}</span><strong>{value}</strong></article>)}</div></div>; }
