import { Check, Languages } from "lucide-react";
import { ThemePanel } from "@/components/ThemePanel";
import { PlayfulSettings } from "@/components/PlayfulSettings";
import { useSettings } from "@/contexts/AppSettingsContext";
import { localeOptions } from "@/i18n";
export default function Settings() { const { language, setLanguage, t, isRtl } = useSettings(); return <div className="site-frame settings-page"><div className="page-intro"><p className="eyebrow">{t("nav.settings")}</p><h1>{t("settings.title")}</h1><p>{t("settings.copy")}</p></div><section className="settings-panel"><div className="section-heading"><span className="icon-orb"><Languages /></span><div><h2>{t("settings.language")}</h2><p>{t("settings.languageCopy")}</p></div></div><div className="language-options">{localeOptions.map((option) => <button key={option.code} onClick={() => setLanguage(option.code)} className={language === option.code ? "selected" : ""}><span>{option.flag}</span><div><strong>{option.nativeName}</strong><small>{option.direction === "rtl" ? "RTL" : "LTR"}</small></div>{language === option.code && <Check size={18} />}</button>)}</div>{isRtl && <p className="rtl-status">{t("language.rtl")}</p>}<p className="settings-hint">{t("language.sync")}</p></section><ThemePanel /><PlayfulSettings /></div>; }
